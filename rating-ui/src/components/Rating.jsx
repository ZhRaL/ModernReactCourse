const Rating = () => {
  return (
    <div style={styles.container}>
      <h2>Rating Component</h2>
    </div>
  );
};

const styles = {
  container: {
    textAlign: 'left',
    fontFamily: 'Arial, sans-serif',
    padding: '20px',
  },
};

export default Rating;
